# Nicole the Astronavigator v1.10.7

今回のアップデートでは、ヘッダーのロゴ表示を新しいデザインへ差し替え、追尾 / 地平モードで天頂付近の方位線が欠ける問題を修正しました。あわせて、天体詳細モーダルを画面中央に表示するよう調整しています。

公開版：  
https://kensukesuga86.github.io/Nicole/

---

## 主な変更

### 🪪 ヘッダーロゴを差し替え

提供された新しいロゴ画像を、ヘッダー表示に反映しました。

- ヘッダーの文字タイトルは表示しない
- ロゴ画像のみを表示
- ロゴ右上の空きスペースに、小さく **`v.1.10.7`** を表示
- フッター側のバージョン表示は削除

ヘッダーまわりはデザイン調整として独立して扱えるようにしてあります。

### 🧭 天頂付近でも方位線を全て描画

前版で高度線は全方位描画に対応していましたが、方位線は視線方向付近のみの生成だったため、追尾 / 地平モードで天頂へ向けた際に反対側の方位線が欠けていました。

v1.10.7では、**方位線も360°全方位から生成**するよう変更し、天頂付近でもグリッドが欠けにくいよう修正しています。

### 🪟 天体詳細モーダルを画面中央に表示

天体カードから開く詳細モーダルは、以前はスマホ環境で下寄りに表示されることがありました。

v1.10.7では、**画面中央基準で表示**するように調整しています。

- 画面中央に表示
- 内容が長い場合はモーダル内部のみスクロール
- 背景タップ / 閉じるボタン / Escape で閉じられる既存仕様は維持

---

## 変更していないもの

今回の更新では以下のロジックには変更を加えていません。

- 星図計算
- 追尾モードの方位補正
- 横向き時の90°補正
- 高仰角180°ジャンプ対策
- 「星図で表示」時の自動スクロール
- 今夜のおすすめ8件表示
- 赤色夜間モード全画面対策
- 赤道儀
- Nicole Cache Manager

---

## PWA / キャッシュ

Service Workerのキャッシュ名前空間を更新しました。

```text
nicole-pwa-v1.10.7
```

キャッシュ管理ポリシー自体には変更ありません。

---

**Version 1.10.7**
