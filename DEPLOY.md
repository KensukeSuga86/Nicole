# Nicole the Astronavigator v1.9.4

更新対象: `index.html` / `sw.js`

## 修正内容

- 追尾モードでスマートフォンを横向きにした際、方位が約90°ずれる問題を修正。
- DeviceOrientation から算出した端末背面の視線ベクトルへ `screen.orientation.angle` を重ねて適用していた二重補正を削除。
- `screen.orientation` は横向き全画面などのレイアウト判定・状態保持には引き続き使用するが、追尾方位の計算には直接加算しない。
- 高仰角で発生する約180°ジャンプ対策、平滑化、追尾ロジックは維持。
- 横向き全画面のワイド表示も維持。
- Service Worker のキャッシュ名前空間を `nicole-pwa-v1.9.4` に更新。

## 確認ポイント

1. 縦持ちで北・東・南・西が概ね一致すること。
2. 同じ方向を向いたまま端末を横向きに回転し、表示方位が約90°飛ばないこと。
3. 横向きのまま北・東・南・西へ向きを変え、方位表示が追従すること。
4. 高仰角へ向けても従来の180°ジャンプ対策が維持されること。

Service Worker cache: `nicole-pwa-v1.9.4`
